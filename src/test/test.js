import should from "should";
import * as babel from "babel-core";
import sourceMapSupport from "source-map-support";
import * as db from "../bashfury";

sourceMapSupport.install();

describe("bashfury", () => {
  beforeEach(() => {
  });

  it(`Evals inline JS`, async () => {
  });
});
